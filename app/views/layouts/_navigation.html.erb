<nav class="fixed inset-y-0 left-0 z-50 w-64 bg-white dark:bg-gray-800 border-r border-gray-200 dark:border-gray-700 hidden lg:block">
  <div class="flex h-16 items-center justify-between px-4 border-b border-gray-200 dark:border-gray-700">
    <%= link_to dashboard_path, class: "text-xl font-bold text-purple-600 dark:text-purple-400 hover:text-purple-700 dark:hover:text-purple-300" do %>Lievik<% end %>
    <div data-controller="theme">
      <button type="button" data-action="click->theme#toggle" data-theme-target="button" class="p-2 text-gray-500 hover:text-gray-700 dark:text-gray-400 dark:hover:text-gray-200 rounded-lg hover:bg-gray-100 dark:hover:bg-gray-700" title="Theme" aria-label="Change colour theme">
        <%# One icon per *mode* (system / light / dark). Which one shows is decided by
            CSS from the data-theme-mode attribute the pre-paint script sets, so the
            server never has to guess. %>
        <svg class="theme-icon theme-icon-system h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.75 17L9 20l-1 1h8l-1-1-.75-3M3 13h18M5 17h14a2 2 0 002-2V5a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
        </svg>
        <svg class="theme-icon theme-icon-light h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z"/>
        </svg>
        <svg class="theme-icon theme-icon-dark h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z"/>
        </svg>
      </button>
    </div>
  </div>

  <div class="flex flex-col h-[calc(100%-4rem)]">
    <div class="flex-1 overflow-y-auto p-4">
      <ul class="space-y-2">
        <li>
          <%= link_to dashboard_path, class: "flex items-center gap-3 rounded-lg px-3 py-2 text-gray-700 dark:text-gray-200 hover:bg-gray-100 dark:hover:bg-gray-700 #{request.path == dashboard_path ? 'bg-gray-100 dark:bg-gray-700' : ''}" do %>
            <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"/>
            </svg>
            Dashboard
          <% end %>
        </li>
        <li>
          <%= link_to sources_path, class: "flex items-center gap-3 rounded-lg px-3 py-2 text-gray-700 dark:text-gray-200 hover:bg-gray-100 dark:hover:bg-gray-700 #{request.path.start_with?('/sources') ? 'bg-gray-100 dark:bg-gray-700' : ''}" do %>
            <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"/>
            </svg>
            Sources
          <% end %>
        </li>
        <li>
          <%= link_to channels_path, class: "flex items-center gap-3 rounded-lg px-3 py-2 text-gray-700 dark:text-gray-200 hover:bg-gray-100 dark:hover:bg-gray-700 #{request.path.start_with?('/channels') ? 'bg-gray-100 dark:bg-gray-700' : ''}" do %>
            <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 20l4-16m2 16l4-16M6 9h14M4 15h14"/>
            </svg>
            Channels
          <% end %>
        </li>
        <li>
          <%= link_to events_path, class: "flex items-center gap-3 rounded-lg px-3 py-2 text-gray-700 dark:text-gray-200 hover:bg-gray-100 dark:hover:bg-gray-700 #{request.path.start_with?('/events') ? 'bg-gray-100 dark:bg-gray-700' : ''}" do %>
            <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"/>
            </svg>
            Events
          <% end %>
        </li>
        <li>
          <%= link_to new_manual_event_path, class: "flex items-center gap-3 rounded-lg px-3 py-2 text-gray-700 dark:text-gray-200 hover:bg-gray-100 dark:hover:bg-gray-700 #{request.path.start_with?('/manual_events') ? 'bg-gray-100 dark:bg-gray-700' : ''}" do %>
            <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"/>
            </svg>
            Add Event
          <% end %>
        </li>
        <li>
          <%= link_to activity_logs_path, class: "flex items-center gap-3 rounded-lg px-3 py-2 text-gray-700 dark:text-gray-200 hover:bg-gray-100 dark:hover:bg-gray-700 #{request.path.start_with?('/activity_logs') ? 'bg-gray-100 dark:bg-gray-700' : ''}" do %>
            <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"/>
            </svg>
            Activity
          <% end %>
        </li>
        <li>
          <%= link_to rag_chat_path, class: "flex items-center gap-3 rounded-lg px-3 py-2 text-gray-700 dark:text-gray-200 hover:bg-gray-100 dark:hover:bg-gray-700 #{request.path.start_with?('/chat') ? 'bg-gray-100 dark:bg-gray-700' : ''}" do %>
            <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"/>
            </svg>
            Ask Your Content
          <% end %>
        </li>
      </ul>
    </div>

    <div class="border-t border-gray-200 dark:border-gray-700 p-4">
      <div class="flex items-center gap-3 mb-4 cursor-pointer group" data-turbo-frame="_top">
        <% if current_user.picture_url.present? %>
          <%= link_to edit_user_path do %>
            <span data-controller="avatar">
              <img src="<%= current_user.picture_url %>" alt="Profile"
                   loading="lazy" referrerpolicy="no-referrer"
                   data-avatar-target="image"
                   data-action="error->avatar#fallback"
                   class="h-10 w-10 rounded-full object-cover hover:ring-2 hover:ring-purple-500 hover:ring-opacity-50 transition-all group-hover:scale-105">
              <span data-avatar-target="fallback" class="hidden h-10 w-10 rounded-full bg-purple-100 dark:bg-purple-900 flex items-center justify-center">
                <span class="text-purple-600 dark:text-purple-400 font-medium"><%= current_user.display_name_or_npub[0].upcase %></span>
              </span>
            </span>
          <% end %>
        <% else %>
          <%= link_to edit_user_path do %>
            <div class="h-10 w-10 rounded-full bg-purple-100 dark:bg-purple-900 flex items-center justify-center hover:ring-2 hover:ring-purple-500 hover:ring-opacity-50 transition-all group-hover:scale-105">
              <span class="text-purple-600 dark:text-purple-400 font-medium">
                <%= current_user.display_name_or_npub[0].upcase %>
              </span>
            </div>
          <% end %>
        <% end %>
        <div class="flex-1 min-w-0">
          <%= link_to edit_user_path, class: "hover:text-purple-600 dark:hover:text-purple-400 transition-colors" do %>
            <p class="text-sm font-medium text-gray-900 dark:text-gray-100 truncate">
              <%= current_user.display_name || current_user.username || "Nostr User" %>
            </p>
            <p class="text-xs text-gray-500 dark:text-gray-400 truncate">
              <%= current_user.npub.truncate(20) %>
            </p>
          <% end %>
          <%= button_to refresh_profile_path, method: :post, class: "mt-1 text-[10px] text-purple-600 dark:text-purple-400 hover:underline flex items-center gap-1" do %>
            <svg class="h-2 w-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"/>
            </svg>
            Refresh Profile
          <% end %>
        </div>
      </div>
      <%= button_to logout_path, method: :delete, class: "w-full flex items-center justify-center gap-2 rounded-lg px-3 py-2 text-sm text-red-600 dark:text-red-400 hover:bg-red-50 dark:hover:bg-red-900/20" do %>
        <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"/>
        </svg>
        Sign out
      <% end %>
    </div>
  </div>
</nav>

<!-- Mobile navigation -->
<div class="lg:hidden" data-controller="mobile-nav" data-action="keydown@window->mobile-nav#closeOnKeydown">
  <!-- Fixed top bar -->
  <div class="fixed top-0 left-0 right-0 z-40 bg-white dark:bg-gray-800 border-b border-gray-200 dark:border-gray-700 px-4 py-3">
    <div class="flex items-center justify-between">
      <button type="button" data-action="click->mobile-nav#toggle" class="p-1 -ml-1 text-gray-500 hover:text-gray-700 dark:text-gray-400 dark:hover:text-gray-200">
        <svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
        </svg>
      </button>
      <%= link_to dashboard_path, class: "text-lg font-bold text-purple-600 dark:text-purple-400" do %>Lievik<% end %>
      <div data-controller="theme">
        <button type="button" data-action="click->theme#toggle" data-theme-target="button" class="p-2 text-gray-500 hover:text-gray-700 dark:text-gray-400 dark:hover:text-gray-200" title="Theme" aria-label="Change colour theme">
          <%# One icon per *mode* (system / light / dark). Which one shows is decided by
              CSS from the data-theme-mode attribute the pre-paint script sets, so the
              server never has to guess. %>
          <svg class="theme-icon theme-icon-system h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.75 17L9 20l-1 1h8l-1-1-.75-3M3 13h18M5 17h14a2 2 0 002-2V5a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
          </svg>
          <svg class="theme-icon theme-icon-light h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z"/>
          </svg>
          <svg class="theme-icon theme-icon-dark h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z"/>
          </svg>
        </button>
      </div>
    </div>
  </div>

  <!-- Backdrop -->
  <div data-mobile-nav-target="backdrop" data-action="click->mobile-nav#close" class="fixed inset-0 z-40 bg-black/50 opacity-0 pointer-events-none transition-opacity duration-300"></div>

  <!-- Slide-out panel -->
  <nav data-mobile-nav-target="panel" class="fixed inset-y-0 left-0 z-50 w-64 bg-white dark:bg-gray-800 border-r border-gray-200 dark:border-gray-700 -translate-x-full transition-transform duration-300 flex flex-col">
    <div class="flex h-14 items-center justify-between px-4 border-b border-gray-200 dark:border-gray-700">
      <%= link_to dashboard_path, class: "text-lg font-bold text-purple-600 dark:text-purple-400", data: { action: "click->mobile-nav#close" } do %>Lievik<% end %>
      <button type="button" data-action="click->mobile-nav#close" class="p-1 text-gray-500 hover:text-gray-700 dark:text-gray-400 dark:hover:text-gray-200">
        <svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
        </svg>
      </button>
    </div>

    <div class="flex-1 overflow-y-auto p-4">
      <ul class="space-y-2">
        <li>
          <%= link_to dashboard_path, class: "flex items-center gap-3 rounded-lg px-3 py-2 text-gray-700 dark:text-gray-200 hover:bg-gray-100 dark:hover:bg-gray-700 #{request.path == dashboard_path ? 'bg-gray-100 dark:bg-gray-700' : ''}", data: { action: "click->mobile-nav#close" } do %>
            <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"/>
            </svg>
            Dashboard
          <% end %>
        </li>
        <li>
          <%= link_to sources_path, class: "flex items-center gap-3 rounded-lg px-3 py-2 text-gray-700 dark:text-gray-200 hover:bg-gray-100 dark:hover:bg-gray-700 #{request.path.start_with?('/sources') ? 'bg-gray-100 dark:bg-gray-700' : ''}", data: { action: "click->mobile-nav#close" } do %>
            <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"/>
            </svg>
            Sources
          <% end %>
        </li>
        <li>
          <%= link_to channels_path, class: "flex items-center gap-3 rounded-lg px-3 py-2 text-gray-700 dark:text-gray-200 hover:bg-gray-100 dark:hover:bg-gray-700 #{request.path.start_with?('/channels') ? 'bg-gray-100 dark:bg-gray-700' : ''}", data: { action: "click->mobile-nav#close" } do %>
            <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 20l4-16m2 16l4-16M6 9h14M4 15h14"/>
            </svg>
            Channels
          <% end %>
        </li>
        <li>
          <%= link_to events_path, class: "flex items-center gap-3 rounded-lg px-3 py-2 text-gray-700 dark:text-gray-200 hover:bg-gray-100 dark:hover:bg-gray-700 #{request.path.start_with?('/events') ? 'bg-gray-100 dark:bg-gray-700' : ''}", data: { action: "click->mobile-nav#close" } do %>
            <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"/>
            </svg>
            Events
          <% end %>
        </li>
        <li>
          <%= link_to new_manual_event_path, class: "flex items-center gap-3 rounded-lg px-3 py-2 text-gray-700 dark:text-gray-200 hover:bg-gray-100 dark:hover:bg-gray-700 #{request.path.start_with?('/manual_events') ? 'bg-gray-100 dark:bg-gray-700' : ''}", data: { action: "click->mobile-nav#close" } do %>
            <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"/>
            </svg>
            Add Event
          <% end %>
        </li>
        <li>
          <%= link_to activity_logs_path, class: "flex items-center gap-3 rounded-lg px-3 py-2 text-gray-700 dark:text-gray-200 hover:bg-gray-100 dark:hover:bg-gray-700 #{request.path.start_with?('/activity_logs') ? 'bg-gray-100 dark:bg-gray-700' : ''}", data: { action: "click->mobile-nav#close" } do %>
            <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"/>
            </svg>
            Activity
          <% end %>
        </li>
        <li>
          <%= link_to rag_chat_path, class: "flex items-center gap-3 rounded-lg px-3 py-2 text-gray-700 dark:text-gray-200 hover:bg-gray-100 dark:hover:bg-gray-700 #{request.path.start_with?('/chat') ? 'bg-gray-100 dark:bg-gray-700' : ''}", data: { action: "click->mobile-nav#close" } do %>
            <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"/>
            </svg>
            Ask Your Content
          <% end %>
        </li>
      </ul>
    </div>

    <div class="border-t border-gray-200 dark:border-gray-700 p-4">
      <div class="flex items-center gap-3 mb-4">
        <% if current_user.picture_url.present? %>
          <%= link_to edit_user_path, data: { action: "click->mobile-nav#close" } do %>
            <span data-controller="avatar">
              <img src="<%= current_user.picture_url %>" alt="Profile"
                   loading="lazy" referrerpolicy="no-referrer"
                   data-avatar-target="image"
                   data-action="error->avatar#fallback"
                   class="h-10 w-10 rounded-full object-cover">
              <span data-avatar-target="fallback" class="hidden h-10 w-10 rounded-full bg-purple-100 dark:bg-purple-900 flex items-center justify-center">
                <span class="text-purple-600 dark:text-purple-400 font-medium"><%= current_user.display_name_or_npub[0].upcase %></span>
              </span>
            </span>
          <% end %>
        <% else %>
          <%= link_to edit_user_path, data: { action: "click->mobile-nav#close" } do %>
            <div class="h-10 w-10 rounded-full bg-purple-100 dark:bg-purple-900 flex items-center justify-center">
              <span class="text-purple-600 dark:text-purple-400 font-medium">
                <%= current_user.display_name_or_npub[0].upcase %>
              </span>
            </div>
          <% end %>
        <% end %>
        <div class="flex-1 min-w-0">
          <%= link_to edit_user_path, class: "hover:text-purple-600 dark:hover:text-purple-400 transition-colors", data: { action: "click->mobile-nav#close" } do %>
            <p class="text-sm font-medium text-gray-900 dark:text-gray-100 truncate">
              <%= current_user.display_name || current_user.username || "Nostr User" %>
            </p>
            <p class="text-xs text-gray-500 dark:text-gray-400 truncate">
              <%= current_user.npub.truncate(20) %>
            </p>
          <% end %>
        </div>
      </div>
      <%= button_to logout_path, method: :delete, class: "w-full flex items-center justify-center gap-2 rounded-lg px-3 py-2 text-sm text-red-600 dark:text-red-400 hover:bg-red-50 dark:hover:bg-red-900/20" do %>
        <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"/>
        </svg>
        Sign out
      <% end %>
    </div>
  </nav>

  <!-- Spacer for fixed header -->
  <div class="h-14"></div>
</div>
